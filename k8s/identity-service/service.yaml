apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: agrosolutions
spec:
  selector:
    app: identity-service
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
