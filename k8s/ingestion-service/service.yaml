apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: agrosolutions
spec:
  selector:
    app: ingestion-service
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
