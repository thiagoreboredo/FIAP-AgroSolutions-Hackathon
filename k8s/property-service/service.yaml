apiVersion: v1
kind: Service
metadata:
  name: property-service
  namespace: agrosolutions
spec:
  selector:
    app: property-service
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
